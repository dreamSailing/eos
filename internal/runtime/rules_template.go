package runtime

func RulesMdTemplate() string {
	return `# Rules

本文件用于约束 AI 在本仓库中的行为与输出，优先用于“如何改这个仓库的代码”。

## 1. 目标
- 代码风格一致
- 修改可验证（能编译/测试）
- 安全合规（不泄露密钥，不做危险操作）

## 2. 代码约定
- 语言/框架：Go
- 命名、目录结构、导入顺序：遵循仓库现有风格
- 不新增注释（除非用户要求）

## 3. 工程命令
~~~bash
go test ./...
~~~

## 4. 修改策略
- 优先最小改动，先修根因再补体验
- 修改前先读现有代码与约定，避免引入新依赖
- 多文件改动需保持一致性

## 5. 交互与输出
- 输出中文
- 代码引用使用文件链接
- 需要时先解释“为什么这样改”
`
}
