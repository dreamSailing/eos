run:
  timeout: 3m
issues:
  exclude-use-default: false
  exclude-rules:
    - path: internal/i18n/.*\.go
      linters:
        - dupl
linters:
  enable:
    - govet
    - staticcheck
    - gosimple
    - ineffassign
    - errcheck
    - dupl
